<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse"
        data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("views.layouts.logo"), root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li><%= link_to t("views.layouts.home"), root_path %></li>
        <% if logged_in? %>
          <% if current_user.admin? %>
            <li><%= link_to t("views.layouts.words"), admin_words_path %></li>
            <li><%= link_to t("views.layouts.categories"),
              admin_categories_path %></li>
          <% else %>
            <li><%= link_to t("views.layouts.words"), words_path %></li>
            <li><%= link_to t("views.layouts.categories"),
              categories_path %></li>
          <% end %>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <% if current_user.admin? %>
            <li><%= link_to t("views.layouts.users"), admin_users_path %></li>
          <% else %>
            <li><%= link_to t("views.layouts.users"), users_path %></li>
          <% end %>
          <li class = "dropdown">
            <a href = "#" class = "dropdown-toggle" data-toggle = "dropdown">
              <%= t "user.account" %>
              <b class = "caret"></b>
            </a>
            <ul class = "dropdown-menu">
              <li>
                <%= link_to t("user.profile"), current_user %>
              </li>
              <li>
                <%= link_to t("user.setting"), edit_user_path(current_user) %>
              </li>
              <li>
                <%= link_to t("user.logout"), logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t("views.layouts.login"), new_session_path,
              class: "glyphicon glyphicon-log-in" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
